#include <iostream>

int main()
{
	int N, X;
	std::cin >> N >> X;

	int A[10000];
	
	for (int i = 0; i < N; i++)
	{		
		std::cin >> A[i];
		
		if (A[i] < X)
		{
			std::cout << A[i] << " ";
		}

	}


	return 0;
}
